//
// Created by Roberto C on 4/12/24.
//

#include "Matematicas.h" // Incluimos el archivo que contiene las declaraciones de las funciones

namespace Matematicas { // Creamos un espacio de nombres para organizar nuestras funciones
    int suma(int a, int b) { // Función que suma dos números enteros
        return a + b; // Devuelve el resultado de la suma
    }

    int resta(int a, int b) { // Función que resta dos números enteros
        return a - b; // Devuelve el resultado de la resta
    }
}
